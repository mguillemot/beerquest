defaults: &defaults
  encoding: utf8
  reconnect: false
  pool: 5

development:
  adapter: sqlite
  database: beerquest_development.db
  username: root
  password:
  host: localhost
  <<: *defaults

test:
  adapter: sqlite
  database: beerquest_test.db
  username: root
  password:
  host: localhost
  <<: *defaults

production:
  adapter: mysql
  database: beerquest_production
  username: beerquest
  password: glouglou
  host: localhost
  <<: *defaults
