%h3.centered= t('bar.messages.title')
%ul.messages
  - @bar.messages.each do |msg|
    %li
      = image_tag msg.account.profile_picture, :alt => msg.account.first_name, :class => 'mini-portrait'
      = image_tag 'mini-biere.png', :alt => t('beers')
      %span.score= msg.score
      -#= "-"
      %br
      %span.name= msg.account.first_name
      -#%br
      -#%span.date= l(msg.updated_at, :format => :short)
      -#&laquo;
      -#= msg.message
      -#&raquo;
