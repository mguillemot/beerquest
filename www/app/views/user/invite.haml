.empty
- invite_msg = t("invite.message#{@invite}")
:javascript
  var initialized = function() {
    FB.ui({
      method: 'apprequests',
      title: "#{t('invite.selector')}",
      message: "#{invite_msg}"
    });
  };