- if @sent_challenges.empty?
  .empty-challenge-grid= t 'home.battle.empty.sent'
- else
  %table.challenge-grid
    = render :partial => 'challenge', :collection => @sent_challenges
.battle-warning= t 'home.battle.warning.sent_html'
